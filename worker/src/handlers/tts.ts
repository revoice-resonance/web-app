/**
 * TTS (Text-to-Speech) handlers.
 *
 * All legacy TTS handlers have been removed in favor of the Cloud TTS
 * (/api/tts/speak) and Voice Clone (/api/tts/voices/clone) endpoints
 * backed by CloudSpeech.
 */